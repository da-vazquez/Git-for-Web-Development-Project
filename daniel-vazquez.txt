https://codepen.io/da-vazquez/pen/YzqmdMW


    1. What is Semantic HTML? I
    It is the code we enter which tells the browser the meaning of the element 

    2. What is HTML used for? 
    It's the standard markup language for creating websites 

    3. What is an attribute and where do we put it? 
    An attribute is a modifier of an html element. It is added to the element's opening tag

    4. What is the h1 tag used for? How many times should I use it on a page?
    It is the heading on a webpage and should only be used once

    5. Name two tags that have required attributes
    <img src for images and <a href for links

    6. What do we put in the head of our HTML document? 
    You put the title and style in our head 

    7. What is an id? 
    An id is used to identify a specific element in html which we can add style to in css 

    8. What elements can I add an id to? 
    It can be added to any element in html

    9. How many times can I use the same id on a page? 
    It should be used only once

    10. What is a class? 
    A class is used to identify multiple elements in html which can be modified at the same time in css

    11. What elements can I add a class to? 
    A class can be added to any element in html (ex: img, p, h2, ul)

    12. How many times can I use the same class on a page? 
    The same class can be used multiple times on a page

    13. How do I get my link to open in a new tab?
    You add "target=blank" after the link address

    14. What is the alt attribute in the image tag used for? 
    The alt is used to specify alternate text if the user is unable to view the image on the webpage

    15. How do I reference an id?
    type a hash character in css to reference an ID (#)

    16. What is the difference between a section and a div
    A section is content that is grouped together. A div is mainly used for styling purposes

    17. What is CSS used for? 
    CSS is used to style the webpage

    18. How to we select an element? Example - every h2 on the page
    
    type the elements name followed by curly brackets. ex:
    
    h2 {

    }

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
    A class represents multiple elements while an id represents only 1 element in html. 

    For a class, you could syle every "p" element in your html to have the same text color and font

    For an id, you could style your "h1" element since it's the only h1 element in your html


    20. How do we select classes in CSS?
    classes are selected by typing a period and the element name in css. for example: .main-container


    21. How do we select a p element with a single class of “human””?
    .human p {

    }

    22. What is a parent child selector? When would this be useful? 
    It's used to match all elements which are the child of a specific "parent" element. This can be useful in identifying and styling specific "child" elements in css.

    23. How do you select all links within a div with the class of sidebar?
    .sidebar a {

    }

    24. What is a pseudo selector?
    It's a selector that selects elements when they're in a certain state. For example, if you hovered your mouse over a certain part of your webpage.

    25. What do we use the change the spacing between lines?
    You would use the line-height property in css. Ex: line-height: 2;

    26. What do we use to change the spacing between letters?
    You would use letter-spacing in css. Ex: letter-spacing: 2px;

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    You would use text-transform in css. Ex: text-transform: uppercase;

    28. How do I add a 1px border around my div that is dotted and black?
    div {
        border: 1px dotted black;
    }

    29. How do I select everything on the page? 
    use the * symbol to select everything on your page

    30. How do I write a comment in CSS?
    /* This is how you write a comment in css */

    31. How do I find out what file I am in, when I am using the command line? 
    pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
    ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    git rm filename

    34. Why should I use version control? 
    It's helpful in keeping track of any changes you've made and to identify which version is currently in development

    35. How often should I commit to github?
    You should commit often, especially after making changes to your project

    36. What is the command we would use to push our repo up to github? 
    git push -u 

    37. Walk me through Lambda's git flow.
    We log into our person account on Github and click on the link for Lambda's assignment. We will then fork and clone the project and create a branch on our terminal. After we have completed all of our tasks for that project, we will add our commit/add commands in terminal. We will then push our branch to github and create a pull request using our personal fork



